function Wrapper(props) {
    return props.children;
}

export default Wrapper;

// Just a replacement for <div></div> to make code simple //

// Can use React.Fragment instead of Wrapper //
