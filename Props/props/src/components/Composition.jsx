import React from "react";

// Build in children property for a parent component //

// Used for refactoring code //

function Composition(props) {
    return <div>{props.children}</div>;
}

export default Composition;
